describe('Bootstrap JS', () => {
  test('should throw error if jQuery is missing', () => {
    const mockJQuery = null;
    expect(() => require('./bootstrap.min.js')).toThrow(
      "Bootstrap's JavaScript requires jQuery"
    );
  });
});
